this folder stores code for managing a custom mturk qualification

it asks users how much they use on twitter

run create_qual.py to generate the custom qualification.
use the --prod argument to generate on prod and not in the requester sandbox (default)

The qualification score works as follows:
use twitter
0= multiple times per day
1= once per day
2= once every few days
3= once per week
4= once every few weeks
5= less often/never
